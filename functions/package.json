{
  "name": "conectados-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Conectados app",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "expo-constants": "~55.0.7",
    "expo-device": "~55.0.9",
    "expo-notifications": "~55.0.12",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
